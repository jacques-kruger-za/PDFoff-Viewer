# Agent Context

This project's agent operating context lives in [`CLAUDE.md`](./CLAUDE.md) — the single source of truth for conventions, commands, structure, and design decisions.

`AGENTS.md` exists only so tools that look for it by name (e.g. Codex) find their way here. Don't duplicate content; edit `CLAUDE.md`.
