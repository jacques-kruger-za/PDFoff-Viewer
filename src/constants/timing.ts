/** Timing constants in milliseconds — single source of truth. */

export const TIMING = {
  AUTO_SCROLL_RESET: 500,
  WHEEL_RENDER_SETTLE: 250,
  WHEEL_ZOOM_STREAK_WINDOW: 140,
  RENDER_COMMIT_TIMEOUT: 250,
  TOOLTIP_DELAY: 400,
} as const;
